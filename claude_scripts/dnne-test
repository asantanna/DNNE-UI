#!/bin/bash
# DNNE Test Suite - Main Command
# Run all tests (unit + integration + coverage)

# Source the command functions
source "$(dirname "${BASH_SOURCE[0]}")/dnne_test_commands.sh"

# Run main test suite
dnne_test "$@"