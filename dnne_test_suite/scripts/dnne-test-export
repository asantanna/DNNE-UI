#!/bin/bash
# DNNE Test Suite - Export System Tests Only
# Run only export system tests

# Source the command functions
source "$(dirname "${BASH_SOURCE[0]}")/commands.sh"

# Run export system tests
dnne_test_export "$@"