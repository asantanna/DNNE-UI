#!/bin/bash
# DNNE Test Suite - Integration Tests Only
# Run only integration tests (slower, 2min timeout)

# Source the command functions
source "$(dirname "${BASH_SOURCE[0]}")/dnne_test_commands.sh"

# Run integration tests
dnne_test_integration "$@"