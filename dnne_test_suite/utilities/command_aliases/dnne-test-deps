#!/bin/bash
# DNNE Test Suite - Dependencies Check
# Check dependencies only (no test execution)

# Source the command functions
source "$(dirname "${BASH_SOURCE[0]}")/dnne_test_commands.sh"

# Run dependency check
dnne_test_deps "$@"