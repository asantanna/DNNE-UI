#!/bin/bash
# DNNE Test Suite - Unit Tests Only
# Run only unit tests (fast, 30s timeout)

# Source the command functions
source "$(dirname "${BASH_SOURCE[0]}")/commands.sh"

# Run unit tests
dnne_test_unit "$@"