# DNNE (Distributed Neural Network Editor) Requirements
# Python 3.8+ required

# Core ML/AI Framework
torch>=2.0.0
torchvision
torchaudio>=2.0.0
numpy
einops>=0.6.0
scipy>=1.10.0
torchsde>=0.2.5

# Computer Vision & Image Processing
Pillow
kornia>=0.6.0
spandrel

# Data Validation & Configuration
pydantic>=2.0
PyYAML>=6.0
omegaconf>=2.3.0
hydra-core>=1.3.0

# Model Storage & Serialization
safetensors

# Web Server & Async
aiohttp
aiofiles
websockets
websocket-client>=1.5.0
requests>=2.28.0

# NLP Support (optional, for text encoding nodes)
transformers>=4.30.0
tokenizers>=0.13.0

# Audio/Video Processing (optional, for media nodes)
av>=10.0.0

# Utilities
psutil
tqdm
pygit2>=1.12.0

# Development & Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.1.0
pytest-cov>=4.0.0

# Note: The following dependencies are part of DNNE-LINUX-SUPPORT:
# - isaacgym: Run setup script: cd isaacgym/python && pip install -e .
# - isaacgymenvs: Run setup script: cd IsaacGymEnvs && pip install -e .
# - rl_games_dnne: Run setup script: cd rl_games_dnne && pip install -e .